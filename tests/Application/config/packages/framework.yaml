framework:
    secret: '%env(APP_SECRET)%'
    form: true
    csrf_protection: true
    session:
        handler_id: ~
    cache:
        pools:
            test.mailer_pool:
                adapter: cache.adapter.filesystem
